from django.db import models


# Models are intentionally kept empty for now.
# The current waste dashboard backend uses a raw SQL query layer because the
# imported MySQL schema has not been finalized or cleaned up yet.
